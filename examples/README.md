# Examples

- `simple_mcp_client.py` - Direct MCP JSON-RPC communication (no deps)
- `langchain_summarize.py` - LangChain agent with MCP tool (requires `OPENAI_API_KEY`)

Run with `python examples/<file>.py` or `uvx examples/<file>.py`.
